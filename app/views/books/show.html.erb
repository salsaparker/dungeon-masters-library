    <h1><%= @book.title %></h1>
    <h3><%= @book.author %></h3>
    <h3><%= @book.genre %></h3>
    <h3><%= @book.number_of_pages %> </h3>
    <h3><%= @book.difficulty_level %></h3>
    <h3><%= @book.stat_bonus %></h3>

      <%= link_to 'edit', edit_book_path(book.id) %> </p>
  <%= link_to 'delete', book_path(book.id), method: :delete %> 